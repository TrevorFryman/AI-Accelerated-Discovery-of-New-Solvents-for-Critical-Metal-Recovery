# =============================================================================
# GSK Solvent G-Score Prediction — Python Dependencies
# =============================================================================
# Install with:  pip install -r requirements.txt
# Tested with Python 3.10+ on Windows
#
# Actual verified versions on development machine:
#   rdkit 2026.03.2 | matplotlib 3.10.8 | seaborn 0.13.2
#   pandas 3.0.0    | numpy 2.4.2        | scipy (latest)
# =============================================================================

# --- Core scientific stack ---
numpy>=2.0
pandas>=2.0
scipy>=1.11

# --- Cheminformatics ---
rdkit>=2023.9.1          # Morgan fingerprints, RDKit descriptors, SMILES validation

# --- Machine learning ---
xgboost>=2.0
scikit-learn>=1.4
shap>=0.44               # Feature importance / Shapley values

# --- Deep learning (ChemBERTa embeddings) ---
torch>=2.1               # CPU or CUDA; install GPU variant separately if needed
transformers>=4.38       # HuggingFace Transformers for ChemBERTa
sentencepiece>=0.2.0     # Tokeniser dependency for ChemBERTa

# --- Visualisation ---
matplotlib>=3.8
seaborn>=0.13

# --- Utilities ---
tqdm>=4.66               # Progress bars
joblib>=1.3              # Parallel / model persistence (used by sklearn)
