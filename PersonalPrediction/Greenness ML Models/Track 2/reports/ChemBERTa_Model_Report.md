# ChemBERTa Embedding Model Report

## 1. Embedding Generation Methodology

- SMILES column used: `solvent_SMILES` (identified dynamically from GSK_dataset.csv)
- Embeddings generated by tokenizing each SMILES string and passing it through the pretrained ChemBERTa encoder.
- Pooling strategy: mean-pooling of the last hidden state across all non-padding tokens, producing one fixed-length vector per molecule.
- Embedding dimensionality: 768 (per molecule)
- Final feature matrix: 154 molecules x 768 dimensions

## 2. ChemBERTa Model Information

- Model: `seyonec/ChemBERTa-zinc-base-v1` (Hugging Face Hub)
- Architecture: RoBERTa-based masked-language-model transformer pretrained on ~770k molecules (SMILES strings) from the ZINC15 database
- Only the base encoder (`AutoModel`) was used for embedding extraction; the masked-language-modeling head was not used (its weights are reported as 'unexpected' on load, which is expected behavior)

## 3. XGBoost Architecture

- Algorithm: XGBoost Regressor (`xgboost.XGBRegressor`)
- Objective: `reg:squarederror`
- Input features: 768 ChemBERTa embedding dimensions
- Target: G-score
- random_state: 42

## 4. Hyperparameters (Best Found via RandomizedSearchCV)

| Hyperparameter | Search Space | Best Value |
|---|---|---|
| n_estimators | [50, 100, 200, 300, 500] | 200 |
| max_depth | [2, 3, 4, 5, 6, 8] | 3 |
| learning_rate | [0.01, 0.02, 0.05, 0.1, 0.2, 0.3] | 0.1 |
| subsample | [0.5, 0.6, 0.7, 0.8, 0.9, 1.0] | 0.9 |
| colsample_bytree | [0.3, 0.4, 0.5, 0.6, 0.7, 0.8, 1.0] | 0.3 |
| min_child_weight | [1, 2, 3, 5, 7, 10] | 7 |

Search configuration: `RandomizedSearchCV` with `n_iter=100`, 5-fold CV (`KFold(n_splits=5, shuffle=True, random_state=42)`), scoring=`neg_root_mean_squared_error`, performed on the training split only (80% of data; the test set was untouched during tuning).

Best training-CV RMSE during search: 1.0900

## 5. Baseline vs Optimized Comparison

| Model | Test RMSE | Test MAE | Test R2 | OOF RMSE | OOF MAE | OOF R2 |
|---|---|---|---|---|---|---|
| Baseline (default params) | 1.2181 | 1.0165 | 0.1763 | 1.2551 | 1.0212 | 0.0295 |
| Optimized (RandomizedSearchCV) | 1.0996 | 0.9075 | 0.3288 | 1.1734 | 0.9533 | 0.1517 |

## 6. Validation Metrics (80/20 Train-Test Split, Optimized Model)

| Set | RMSE | MAE | R2 |
|---|---|---|---|
| Train | 0.0233 | 0.0174 | 0.9997 |
| Test | 1.0996 | 0.9075 | 0.3288 |

## 7. Cross-Validation Results (5-Fold OOF, Full Dataset, Best Hyperparameters)

| Metric | Value |
|---|---|
| RMSE | 1.1734 |
| MAE | 0.9533 |
| R2 | 0.1517 |

## 8. Fit Assessment

- Train R2: 0.9997
- 5-fold OOF R2: 0.1517
- Gap: 0.8480
- **Assessment: Overfitting**

## 9. Advantages

- ChemBERTa embeddings require no manual feature engineering or descriptor selection — a single pretrained model produces a fixed-length representation for any valid SMILES string.
- The embeddings encode learned chemical/structural context from a large pretraining corpus (~770k molecules), which can in principle capture subtle structure-property relationships not represented by hand-crafted descriptors.
- The pipeline is fully reproducible (fixed random_state=42 for splitting, cross-validation, and model fitting).

## 10. Limitations

- With 768 dense features and only 154 samples, this pipeline operates in a severe p >> n regime, leading to substantial overfitting (gap = 0.8480) even after hyperparameter tuning.
- Generalization performance (5-fold OOF R2 = 0.1517) was lower than the Morgan Fingerprint and RDKit Descriptor baselines on this dataset, suggesting that for this small, structurally narrow solvent dataset, high-dimensional pretrained embeddings did not translate into a clear predictive advantage without further dimensionality reduction or regularization.
- Mean-pooling over token embeddings is a simple aggregation strategy; alternative approaches (e.g., using the `<s>`/CLS token, fine-tuning the transformer end-to-end, or pooling intermediate layers) were not explored.
- Embedding generation requires downloading and running a transformer model (GPU recommended for larger datasets), adding computational overhead compared to RDKit descriptors.

## 11. Recommendations for Future Solvent-Property Prediction Studies

- For small datasets (on the order of hundreds of samples), prefer low-dimensional, domain-informed descriptors (e.g., RDKit physicochemical descriptors) or apply dimensionality reduction (PCA, feature selection) to high-dimensional embeddings before modeling.
- If using ChemBERTa or similar pretrained embeddings, consider stronger regularization (shallow trees, high `min_child_weight`, low `colsample_bytree`/`subsample`) or linear models with L1/L2 penalties (e.g., Ridge, Lasso) which may handle p >> n settings more gracefully than tree ensembles.
- Collecting additional labeled solvent data would likely benefit the embedding-based pipeline more than the lower-dimensional descriptor pipelines, since high-capacity representations need more data to avoid overfitting.
- A hybrid approach — combining compact RDKit descriptors with a reduced-dimensionality projection of ChemBERTa embeddings (e.g., via PCA) — could be explored as a follow-up study to capture complementary information from both representations.

## 12. Output Files

- Best model: `models/chemberta_xgboost_model.pkl`
- Best hyperparameters: `models/chemberta_best_params.json`
- Baseline plots: `results/ChemBERTa/learning_curve.png`, `results/ChemBERTa/prediction_residual_plots.png`
